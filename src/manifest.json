{
  "name": "Scrollbar Fix for Google Calendar™",
  "version": "1.1",
  "description": "Removes the unnecessary scrollbar in Google Calendar.",
  "content_scripts": [
    {
      "matches": [
        "http://*.google.com/calendar/*",
        "https://*.google.com/calendar/*"
      ],
      "js": [
        "content.js"
      ],
      "run_at": "document_end"
    }
  ]
}